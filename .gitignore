# Secrets — NEVER commit your real .env (nor the backups `npm run setup` keeps)
.env
.env.backup-*

# Bot state (positions, P&L history)
data/

# Node
node_modules/
npm-debug.log*

# macOS
.DS_Store

# graphify: local interpreter/root paths and extraction cache are machine-specific
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/cache/
